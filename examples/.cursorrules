# .cursorrules — Cursor

<!-- generated by ContextForge · RULESET_VERSION 2026-09-26 · source: rule-based -->
<!-- Review before committing. Items marked (inferred) were guessed from filenames. -->

## Project
- Languages: TypeScript, TypeScript (React), JavaScript, YAML
- Package manager: npm
- Framework: Next.js
- Test runner: unknown (test files present) (inferred)
- CI: GitHub Actions
- File count in supplied tree: 17

## Commands
- Build: `npm run build`
- Test: `npm run test`
- Lint: `npm run lint`

## Layout
- `.github/` — GitHub metadata and workflows
- `.github/workflows/` — project directory
- `prisma/` — database schema
- `src/` — application source
- `src/app/` — application routes
- `src/app/api/` — HTTP handlers
- `src/app/api/users/` — project directory
- `src/components/` — UI components
- `src/hooks/` — React hooks
- `src/lib/` — shared libraries
- `src/types/` — type declarations
- `tests/` — tests

## Entry points
- `src/app/layout.tsx`
- `src/app/page.tsx`

## Conventions
- Match the naming and file layout already present in the tree above.
- Prefer editing existing modules over creating new ones.
- Run the lint and test commands before claiming a change is complete.

## Boundaries
- Do not edit or generate files under these paths:
  - `out`
  - `.git`
- Do not commit secrets, keys, or environment files.
- If a required fact is missing, ask instead of assuming.

## Open questions for the maintainer
- none
